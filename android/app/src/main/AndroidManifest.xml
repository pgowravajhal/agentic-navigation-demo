<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".NaviApplication"
        android:allowBackup="true"
        android:label="Agentic Navigator"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:usesCleartextTraffic="true">

        <!-- HERE SDK credentials (loaded from local.properties via manifestPlaceholders) -->
        <meta-data
            android:name="com.here.sdk.access_key_id"
            android:value="${HERE_ACCESS_KEY_ID}" />
        <meta-data
            android:name="com.here.sdk.access_key_secret"
            android:value="${HERE_ACCESS_KEY_SECRET}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
